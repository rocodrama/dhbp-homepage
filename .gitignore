# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.firebase

# 환경 변수. 지금 쓰는 .env.local 은 위 *.local 에 우연히 걸려서 무시되고 있었다 —
# .env / .env.production 을 새로 만들면 그냥 커밋된다. 이름을 명시해 둔다.
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 브라우저 자동화 스크래치
.playwright-mcp/
*.png.tmp

# 개인 브라우저에 붙는 MCP 설정 — 저장소에 공유하지 않는다
.mcp.json
